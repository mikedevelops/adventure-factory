/**
 * @typedef {Object} PassageChoice
 * @param {List<PassageOption>} options
 */

/**
 * @param {List<PassageOption>} options
 * @return {PassageChoice}
 */
export const createChoice = options => ({
  options
});
